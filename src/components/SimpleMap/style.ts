import styled from "styled-components";

export const MapWrapper = styled.div`
  position: relative;

  .gmap {
    width: 100%;
    height: 600px;
    background: var(--whiter);
    border-radius: 20px;
  }
`;