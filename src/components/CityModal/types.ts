export enum Steps {
    Choosing,
    Detailing
}

export enum ShippingMethod {
    Delivery,
    Pickup
}

export enum Changes {
    DeliveryToPickup,
    PickupToPickup,
    PickupToDelivery,
    DeliveryToDelivery
}