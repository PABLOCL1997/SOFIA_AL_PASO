export enum Steps {
  Choosing,
  Detailing,
}

export enum ShippingMethod {
  Delivery,
  Pickup,
  Express,
  Store,
}

export enum Changes {
  DeliveryToPickup,
  PickupToPickup,
  PickupToDelivery,
  DeliveryToDelivery,
}
